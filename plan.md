# GST Confidence Recovery Plan

Updated: 2026-06-22

## Current Verified State

The active blocker is no longer materialization coverage. Current generated artifacts show:

- Rules materialization: `384` applied, `0` coverage gaps.
- Act materialization: `132` applied, `0` coverage gaps.
- Forms materialization: `608` applied, `0` coverage gaps.
- Reconciliation: `56` unresolved, all `true_substantive_mismatch`.
- Confidence tiers: `A=92 B=577 C=56 D=0` across `725` components.
- Tests: `343` passing.

The previous plan content about rules/act/forms coverage closure is stale.

## Active Work Queue

Use `derived/version_history/cgst-rules-2017/reconciliation_unresolved_audit.json` as the working queue. It is generated by `python3 main.py version reconcile` and attributes each unresolved component to source/event candidates.

Audit class counts:

- `compound_split_needed`: `40`
- `missing_substitution`: `12`
- `missing_insert_child`: `2`
- `manual_backfill_needed`: `2`

There are no current Tier D rule components. Rules `9`, `23`, `39`, `87`,
`88d`, `96b`, and `130` were reduced from Tier D to Tier C/B by consuming
deterministic materialization-manifest evidence: applied events, already
reflected events, misrouted-but-applied events, and source-backed context gaps.
Their true substantive mismatches remain visible and actionable where applicable.
Rule `46a` was repaired from source-backed Notification 45/2017 and 26/2022
payloads, then reconciled as format-only checkpoint drift caused by duplicated
TaxInformation footnote and proviso-label structure.
Rules `97a` and `107a` now reconcile as format-only checkpoint drift caused by
short duplicated TaxInformation rule-label/heading prefixes. Rule `120a` remains
unresolved because its reconstructed text is missing an opening legal sentence,
and this is regression-covered so the normalizer cannot hide it.
Rule `121` now reconciles as format-only parser drift after stripping a trailing
standalone chapter heading bleed from substantive comparison.
Rules `124` and `125` now materialize as omitted from source-backed Notification
24/2022 clause 2(b), which names both rules but had been compiled as one
document-scope omission attached to Rule 122. Rule `109A` now materializes both
Notification 60/2018 sub-rule substitutions against the parent Rule 109A text
and reconciles as format-only checkpoint drift. Rule `31A` now materializes the
complete source-backed 2018 insertion, the clean 2020 sub-rule (2)
substitution, and the later 2025 rate substitution; it reconciles as
format-only checkpoint drift with regression coverage preventing notification
note/signature contamination.
Rule `107` now reconciles as format-only after baseline decontamination strips
the trailing `Chapter - XIII Appeals and Revision` heading that the source
repair stage had glued to the rule body.
Rule `96A` now materializes the source-backed Notification 51/2017 provisos,
the Notification 12/2024 clause (b) substitution, and the reviewed 2024
`FORM GSTR-1A` splice against the parent Rule 96A text. The earlier compiler
prefix match had attached the 2024 clause (b) substitution to Rule 96; this is
now regression-covered.
Rule `129` now receives the source-backed Notification 29/2018
anti-profiteering substitution split from the same compound event that also
amends Rule `130`. Rule `130` now applies the parent-level substitution and
reconciles as format-only checkpoint drift after stripping TaxInformation
`Substituted for the word ... vide Notification...` annotation text.
Rule `38` now materializes the source-backed Notification 19/2022 clause
`(a)(ii)` omission, clause `(c)` substitution, and clause `(d)` omission. It
now reconciles as format-only checkpoint drift caused by TaxInformation
annotation markers.
Rule `119` now retargets the Notification 34/2017 time-limit substitution from
the incorrectly compiled Rule `117` target to Rule `119`; the remaining Rule
`119` checkpoint difference is still visible in the `missing_substitution`
queue. Rule `120A` now materializes its source-backed Notification 34/2017
insertion and Notification 36/2017 marginal-heading update; it reconciles as
format-only checkpoint drift.
Rule `49` now materializes the source-backed electronic bill of supply
signature proviso from Notification 74/2018 and the QR-code proviso from
Notification 31/2019; it reconciles as format-only checkpoint drift.
Rule `94` now splits Notification 38/2023 into a parent renumbering of sub-rule
`(1)` and the source-backed insertion of sub-rule `(2)`, with reconciliation
normalization stripping TaxInformation heading/inline-marker noise.

## Execution Order

1. Keep Tier D at zero.
   - Do not downgrade unresolved legal-text mismatches silently.
   - Source-backed context gaps may stay Tier C only when the materialization
     manifest preserves the exact event and target evidence.

2. Process `compound_split_needed`.
   - Improve compiler splitting for repeated patterns that affect multiple components.
   - Keep parent UNKNOWN rows as containers when residue remains.
   - Create deterministic child events with source span/hash provenance.

3. Process the now-separated `missing_substitution` and `missing_insert_child` queues.
   - Use the candidate events and source documents already attached in the audit rows.
   - Prefer compiler/materializer generalizations when the same source shape repeats.

4. Use manual backfills only for narrow hard cases.
   - Limit backfills to rows in the unresolved audit.
   - Require source document, source span/hash, effective date, target component, operation, and payload.

5. Regenerate confidence/reporting after each batch.
   - Do not upgrade a component based on similarity alone.
   - Residual C/D components must retain exact diagnostics and source attribution.

## Verification

Run after every repair batch:

```bash
python3 main.py version materialize
python3 main.py version materialize --target-work /in/union/acts/cgst-act-2017
python3 main.py version materialize-forms
python3 main.py version reconcile --target-work /in/union/rules/cgst-rules-2017 --checkpoint-path derived/version_history/reconciliation_sources/current-taxinformation/checkpoint.xml --checkpoint-date 2026-06-17 --output derived/version_history/cgst-rules-2017/reconciliation_report.json --version-dir derived/version_history/cgst-rules-2017
python3 main.py version confidence-tiers
python3 main.py version phase3-backlog
python3 main.py version html-report
python3 -m pytest tests/test_canonical_corpus.py -q
```

Acceptance for 95%+ citeable confidence:

- Rules, Act, and Forms coverage gaps stay at `0`.
- Tier D stays `0`.
- `A+B` is at least `95%` of total components.
- Every remaining Tier C item has audit attribution and source diagnostics.
